import  React from 'react';
import ReactDOM from 'react-dom';
import App from './App'

const selRoot = document.getElementById('root');
const root = ReactDOM.createRoot(selRoot);

root.render(<App />)